#include <vector>

void ChoiceOfPlayer(std::vector<std::vector<char>> &board, int size, int turn);
bool CheckResult(std::vector<std::vector<char>> board, int size);
bool CheckTie(std::vector<std::vector<char>> board);
